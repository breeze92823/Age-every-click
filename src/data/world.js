// Level layout is data, not a Blender file — trimmed to a single flat
// ground plane for this basic template (Ice-Skate's data/hub.js is the
// fuller version of this pattern for an island + water + props level).

export const GROUND_Y = 0
export const GROUND_WIDTH = 60
export const GROUND_DEPTH = 60

export const SPAWN = { x: 0, y: GROUND_Y, z: 8 }
